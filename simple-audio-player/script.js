const audioPlayer = document.getElementById("audioPlayer");const volumeControl = document.getElementById("volumeControl");// Define the tracks with publicly accessible URLsconst tracks = {    //motivation: "https://www.soundhelix.com/examples/mp3/SoundHelix-Song-1.mp3",   // Example public file    //concentration: "https://www.soundhelix.com/examples/mp3/SoundHelix-Song-2.mp3", // Example public file    //relax: "https://www.soundhelix.com/examples/mp3/SoundHelix-Song-3.mp3"         // Example public file    motivation: "./motivation.mp3",    concentration: "./concentration.mp3",    relax: "./relax.mp3",};// Function to play the audiofunction playTrack(track, button) {    audioPlayer.src = track;    audioPlayer.play();    button.textContent = "⏸️"; // Set the button to pause symbol}// Function to reset all buttons to "play" symbolfunction resetButtons() {    document.getElementById("motivationButton").textContent = "▶️";    document.getElementById("concentrationButton").textContent = "▶️";    document.getElementById("relaxButton").textContent = "▶️";}// Function to toggle play/pause for the buttonfunction togglePlayPause(button, track) {    if (audioPlayer.src.includes(track)) {        if (audioPlayer.paused) {            audioPlayer.play();            button.textContent = "⏸️"; // Pause symbol        } else {            audioPlayer.pause();            button.textContent = "▶️"; // Play symbol        }    } else {        // If a different track is selected, reset other buttons and play the new track        resetButtons();        playTrack(track, button);    }}// Event listeners for each buttondocument.getElementById("motivationButton").addEventListener("click", function() {    togglePlayPause(this, tracks.motivation);});document.getElementById("concentrationButton").addEventListener("click", function() {    togglePlayPause(this, tracks.concentration);});document.getElementById("relaxButton").addEventListener("click", function() {    togglePlayPause(this, tracks.relax);});// Volume ControlvolumeControl.addEventListener("input", function() {    audioPlayer.volume = this.value;});// When the audio ends, reset all buttons to the "play" symbolaudioPlayer.addEventListener("ended", function() {    resetButtons();});